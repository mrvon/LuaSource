-- local mylib = require "mylib"

max = math.max
min = math.min
-- My Conf
width = 1024
height = 2048

print(string.format("Lua> width: %d, height: %d", width, height))

background = RED

print(string.format("Lua> Red: %d, Green: %d, Blue: %d", background.r, background.g, background.b))

function f(x, y)
    return x + y
end

function sf(x, y)
    return x .. y
end

function less_than(x, y)
    if x < y then
        return true
    else
        return false
    end
end
